{
  "name": "react-from-scratch",
  "version": "1.0.0",
  "description": "script goes in the body - at the end script goes at the end of the body      -ensures the div id=\"root\" is loaded before the script tries to use that",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
